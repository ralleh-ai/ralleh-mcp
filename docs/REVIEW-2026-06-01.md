# Code Review — 2026-06-01

## Scope

Reviewed the initial Ralleh MCP scaffold for performance, testability, health checks, VPS-local security, upgradeability, and backup/restore posture.

## Findings fixed in this pass

1. **No health checks** — added `--health` one-shot JSON and `--health-server` local-only HTTP endpoints: `/healthz`, `/readyz`, `/version`.
2. **No local bind guard** — added `netguard.ValidateLocalListen`; health server rejects `0.0.0.0` and non-loopback IPs by default.
3. **Executor needed explicit partial-result tests** — added bounded executor tests for partial success/error behavior and source clamping.
4. **Travel registry needed parity with shop registry** — added typed travel source/collection registry and rejection tests.
5. **Ops posture not documented** — added install, upgrade, backup, restore, and systemd hardening artifacts.

## Remaining recommended improvements

- Add real MCP protocol handlers once the first tool contract is locked.
- Add runtime-loaded YAML config with schema validation.
- Add source health/circuit-breaker persistence.
- Add Prometheus metrics on a local-only endpoint after the first actual search adapter lands.
- Add integration tests with fake source adapters and httptest upstreams.
- Add release workflow with pinned Go version and checksum generation.

## Current risk posture

Good for scaffold/prototype. Not production search-ready until adapter fetching, source health, config loading, and MCP protocol handlers are implemented and tested.
